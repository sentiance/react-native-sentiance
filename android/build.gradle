apply plugin: 'com.android.library'

buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.1.1'
    }
}

android {
    compileSdkVersion 26
    buildToolsVersion "27.0.3"
    publishNonDefault true
    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 22
        versionCode 1
        versionName "1.0"
    }
    lintOptions {
        abortOnError false
    }
    productFlavors {}
}

repositories {
    mavenCentral()
    maven {
        url "http://repository.sentiance.com"
    }
    google()
}

dependencies {
    implementation 'com.facebook.react:react-native:+'
    api('com.sentiance:sdk:4.2.5-R@aar') {transitive = true}
}
