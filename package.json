{
  "name": "react-native-sentiance",
  "version": "6.0.0-beta.5",
  "description": "React Native libraries for the Sentiance SDK",
  "scripts": {
    "publishLocal": "cd packages && cd crash-detection && npm publish --registry http://localhost:4873/ && cd .. && cd legacy && npm publish --registry http://localhost:4873/ && cd .. && cd core && npm publish --registry http://localhost:4873/ && cd .. && cd user-context && npm publish --registry http://localhost:4873/ && cd .. && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1",
    "fetch:npm_tag": "node ./scripts/determine-version-tag.js",
    "lint": "npm run lint --workspaces"
  },
  "keywords": [
    "react-native",
    "sentiance",
    "core",
    "crash-detection",
    "user-context",
    "legacy"
  ],
  "workspaces": [
    "packages/core",
    "packages/crash-detection",
    "packages/user-context",
    "packages/legacy"
  ],
  "author": "",
  "license": "",
  "peerDependencies": {
    "react-native": "^0.60.3"
  },
  "homepage": "https://github.com/sentiance/react-native-sentiance#readme",
  "bugs": "https://github.com/sentiance/react-native-sentiance/issues",
  "repository": "github:sentiance/react-native-sentiance",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.12.0",
    "eslint": "^8.9.0",
    "jsdoc": "^3.6.10",
    "jsdoc-to-markdown": "^7.1.1",
    "prettier": "^2.6.2",
    "prettier-plugin-jsdoc": "^0.3.38",
    "typescript": "^4.5.5",
    "verdaccio": "^5.10.2"
  }
}
